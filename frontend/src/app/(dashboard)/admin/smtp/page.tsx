import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminSmtpPage(): React.JSX.Element {
  return <ComingSoon title="SMTP Configuration" description="Configure the outgoing mail server used for notifications." />;
}
